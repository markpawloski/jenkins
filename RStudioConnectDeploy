pipeline{
  agent any
  stages{
    stage("Build"){
      steps{
        echo "${WORKSPACE}"
        sh '''#!/bin/bash
          tar czf bundel.tar.gz manifest.json testmarkdown.Rmd
        '''
      }
    }
  }
}
